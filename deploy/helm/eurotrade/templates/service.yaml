apiVersion: v1
kind: Service
metadata:
  name: {{ include "eurotrade.fullname" . }}
  labels:
    {{- include "eurotrade.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "eurotrade.selectorLabels" . | nindent 4 }}
